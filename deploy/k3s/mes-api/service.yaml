apiVersion: v1
kind: Service
metadata:
  name: ames-mes-api
  namespace: ames-dev
  labels:
    app: ames-mes-api
    app.kubernetes.io/part-of: ames
spec:
  type: NodePort
  selector:
    app: ames-mes-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30100
