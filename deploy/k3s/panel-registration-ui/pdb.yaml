apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ames-panel-registration-ui
  namespace: ames-dev
  labels:
    app: ames-panel-registration-ui
    app.kubernetes.io/part-of: ames
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: ames-panel-registration-ui
